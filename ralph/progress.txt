# Ralph Progress — GeekVault SaaS Redesign Plan
# Branch: ralph/saas-redesign-plan
# Started: 2026-03-21

## Codebase Patterns
- CSS custom properties defined in `src/web/src/index.css` with `:root` (light) and `.dark` scopes
- Sidebar has its own token set (`--sidebar-*`) separate from main theme
- Components in `ds/` are design-system-level; `ui/` holds shadcn/ui primitives
- All pages use react-i18next — every UI string needs keys in both `en.json` and `pt.json`
- Feature pages are in `src/web/src/features/{domain}/` with co-located sub-components
- Animation uses Framer Motion wrappers from `ds/motion.tsx` — respects `prefers-reduced-motion`
- Layout breakpoints: mobile (<768px uses Header+Sheet), tablet/desktop (>=768px uses Sidebar+TopToolbar)
- Sidebar collapse state persisted in localStorage key `"geekvault-sidebar-collapsed"`
- Chart colors in dashboard are hardcoded — not from CSS tokens (dark mode issue)
- Empty states inconsistent: some pages show EmptyState component, dashboard sub-sections return null
- New palette uses Tailwind "stone" scale (warm grays) — all neutrals must be stone, not slate/zinc/gray
- Amber accent: #D97706 (light) / #F59E0B (dark) — always brighter on dark backgrounds
- New semantic tokens: `--warning` and `--info` (not in current codebase, will need adding)
- Display font changing from Nunito → Plus Jakarta Sans (warmer geometric, more premium)
- Body font remains Inter — proven for UI, has tabular figures
- Shadow tokens are NEW — current codebase has zero shadow custom properties
- Dark mode shadows use `rgba(0,0,0,...)` at higher opacity; light mode uses warm-tinted `rgba(28,25,23,...)`
- Border radius scale tightened: --radius-md now 8px (was 10px), --radius-lg now 12px (was 16px)
- `--radius` base variable being removed — all radius tokens are now explicit values
- Spacing scale adds --space-0.5, --space-1.5, --space-16, --space-20, --space-24 (new tokens)
- Motion: 5 duration tokens (instant 50ms, fast 150ms, normal 250ms, slow 400ms, deliberate 600ms)
- Motion: 4 spring configs (default, gentle, bouncy, stiff) — always specify `type: "spring"` in Framer Motion
- Motion: Reduced-motion check is module-level in motion.tsx — all new animation wrappers must use `getVariants()`
- Motion: Skeleton pulse must use custom CSS keyframe, NOT Tailwind `animate-pulse` (no reduced-motion respect)
- Motion: Page transitions use AnimatePresence `mode="wait"` — sequential exit→enter, no crossfade
- cmdk `CommandDialog` wraps Radix Dialog — animate via `forceMount` + AnimatePresence, not direct Framer Motion
- Command palette recent items use `sessionStorage` key `"geekvault-recent-pages"` — resets per session
- Page section spacing: `--space-8` (32px) desktop, `--space-6` (24px) mobile — consistent across all pages
- Chart fills must use `hsl(var(--chart-N))` syntax, never hardcoded hex — automatic dark mode support
- Collection cards with onClick on divs need `tabIndex={0}` + `role="link"` + `onKeyDown` for keyboard access
- design-pages.md is the central file for all page-level designs — append new page sections to it
- Grid cards on collections page use `--radius-xl` (16px) for gallery feel — other pages use `--radius-lg` (12px)
- Gradient fallback rotation: `index % 4` for visual variety when cover images are missing
- Continuous hover effects (image zoom) should use CSS transitions, NOT Framer Motion — smoother for mouse-tracking states
- List view tables with image-heavy data should include small thumbnails (40×40px) in the primary column
- Copy card hover-reveal actions need `sm:opacity-0 sm:group-hover:opacity-100` for mobile touch support (not plain `opacity-0`)
- Breadcrumbs only used on catalog item detail page (3 levels deep) — all other pages use sidebar highlight + back links
- Financial values: use `font-variant-numeric: tabular-nums` on price/value displays (Inter supports natively)
- Lightbox components must use `createPortal` to escape overflow containers and keyboard focus trap
- Inline editing: optimistic update + rollback — no success toast, toast on error only
- Image upload zone: `grid-cols-4 sm:grid-cols-5` for preview grid (not flex-wrap — prevents ragged rows)
- Hover-reveal buttons: add `group-focus-within:opacity-100` alongside `group-hover:opacity-100` for keyboard accessibility
- Currency prefix pattern: `absolute left-3 pointer-events-none` + `pl-7` on input
- Progress ring SVG: `stroke-dasharray` = `{pct/100 * circumference} {circumference}`, rotate -90deg to start at top
- Completion tier thresholds: <50% muted, 50-99% accent, 100% success — used across rings, bars, and text colors
- Dashed border = incomplete/missing slot; solid border = owned/acquired — visual language for sets
- Missing items use `bg-card/50` to visually recede; owned items get `border-l-3 border-l-success` left stripe
- Wishlist quick-add: optimistic "Added" → "In Wishlist" (disabled) — no toast on success, toast on error only
- Wishlist cards get subtle `hover:shadow-md` (no translate lift) — data cards, not navigable destinations (same as copy cards)
- Chevron rotation animation (single icon + Framer Motion rotate) is smoother than swapping ChevronDown/ChevronRight icons
- Notes fields should use Textarea (not Input) for multi-line content — `rows={3} resize-y`
- Priority color dots in Select options: `w-2 h-2 rounded-full` inline with flex layout inside SelectItem
- Date-range client-side filtering: use `item.id` descending as creation-order proxy when `createdAt` is unavailable

---

## 2026-03-21 - US-001
- Completed full design audit of the frontend
- Files created: `tasks/design-audit.md`
- **Learnings for future iterations:**
  - Auth layout uses hardcoded gradient colors, not CSS variables — needs fixing in redesign
  - Chart colors (#1B3A6B, #E8A838) are hardcoded in charts-section.tsx — nearly invisible in dark mode
  - No shadow/elevation tokens exist — all shadows via Tailwind utilities with no dark mode adaptation
  - Stats row grid jumps 2-col → 5-col, leaving orphan card on tablet
  - Card click handlers use onClick on divs — not keyboard accessible
  - `prefers-reduced-motion` is respected in motion.tsx but NOT in Tailwind animate-pulse/animate-spin
  - Spacing tokens (--space-*) exist but are rarely used in practice — most spacing is direct Tailwind utilities
---

## 2026-03-21 - US-002
- Defined new color palette and brand identity: "Warm Obsidian" direction
- Proposed 3 palette directions (Slate & Emerald, Warm Obsidian, Arctic Violet), selected Warm Obsidian
- Full light + dark mode specification with stone/warm gray family and deep amber accent
- Sidebar tokens for both modes (always-dark sidebar with amber active indicators)
- 8-color chart palette for recharts (light + dark variants)
- Interactive state colors: focus ring, selection highlight, hover overlays
- Dark mode elevation strategy with warm surface stacking
- WCAG AA contrast ratios verified for all key pairings
- Files created: `tasks/design-tokens.md` (Color Palette section)
- **Learnings for future iterations:**
  - Tailwind's "stone" scale (warm grays) is the foundation — all neutrals must use stone, not slate/zinc/gray
  - Amber accent shifts from #D97706 (light) to #F59E0B (dark) — brighter on dark backgrounds
  - Primary button inverts between modes: dark-on-light → light-on-dark (not just accent swap)
  - Dark mode needs more opaque shadows (rgba 0.5) since subtle shadows vanish on dark backgrounds
  - `focus-visible` (not `focus`) should be used for focus rings to avoid rings on mouse click
  - Chart colors need separate light/dark values — brighter variants for dark mode backgrounds
  - Warning token (#E89B2D / #FBBF24) is new — current codebase lacks it, will need adding
  - Info token (#2563EB / #3B82F6) is new — current codebase also lacks it
---

## 2026-03-21 - US-003
- Defined typography, spacing, border-radius, and elevation tokens
- Appended Typography, Spacing, Border Radius, Elevation, and CSS Property Reference sections to `tasks/design-tokens.md`
- Font pairing: Plus Jakarta Sans (display) + Inter (body) — replaces Nunito + Inter
- Full type scale: display-xl/lg, h1-h4, body-lg/base/sm, caption, overline with exact size/weight/line-height/letter-spacing
- Spacing: 4px base, 15 tokens from 2px (--space-0.5) to 96px (--space-24)
- Border radius: 7 tokens from none to full, tightened --radius-md (10px→8px) and --radius-lg (16px→12px)
- Shadows: 4 levels (sm/md/lg/xl) with warm-tinted light mode and high-opacity dark mode variants
- Complete CSS custom property reference for implementation copy-paste
- **Learnings for future iterations:**
  - Plus Jakarta Sans available via `@fontsource/plus-jakarta-sans` — same install pattern as existing fonts
  - Shadow tokens need to be in both `:root` and `.dark` blocks (mode-specific), while radius/spacing go in `@theme inline` (mode-independent)
  - Tailwind CSS v4 shadow integration: define `--shadow-*` in `:root`/`.dark`, then reference via `--shadow-*` in `@theme inline`
  - `--radius` base variable (currently 0.625rem) used by `--radius-xl` calc — removing it requires making all radius tokens explicit
  - Current spacing tokens only go up to --space-12; page-level layout needs --space-16/20/24 for generous breathing room
  - Use `font-variant-numeric: tabular-nums` on numeric data columns — Inter supports this natively
---

## 2026-03-21 - US-004
- Defined complete animation and motion language for the redesign
- Files created: `tasks/design-motion.md`
- Sections: Animation Principles, Duration Tokens, Easing Curves (CSS + Framer Motion springs), Page Transitions, Component Entrance Patterns, Hover/Press Micro-Interactions, Loading Patterns (skeleton/spinner/progress), Overlay & Panel Animations (modal/sheet/toast/dropdown), Drag-and-Drop Feedback, Reduced Motion Strategy
- 5 animation principles aligned with Warm Obsidian identity (purposeful, fast, physics-based, consistent directionality, warm and grounded)
- 5 duration tokens (50ms–600ms), 4 CSS easing curves, 4 Framer Motion spring configs
- **Learnings for future iterations:**
  - Current motion.tsx already has a solid reduced-motion pattern (module-level check + noopVariants) — extend it, don't replace
  - Page transition uses `mode="wait"` in AnimatePresence — exit completes before enter starts (no crossfade)
  - Current stagger delay is 60ms — keeping that as default; add 40ms fast variant for dense lists
  - Framer Motion springs need explicit `type: "spring"` — omitting it defaults to tween
  - CSS `transition-duration` override in reduced-motion media query needs `!important` to beat inline styles
  - Skeleton pulse should NOT use Tailwind's `animate-pulse` — it doesn't respect `prefers-reduced-motion` (noted in audit)
  - Toast animations (sonner) may need custom configuration to match the motion system — sonner has its own animation props
---

## 2026-03-21 - US-005
- Designed app shell and navigation layout with full paradigm evaluation
- Evaluated 4 layout paradigms (sidebar, top-nav, bottom-nav, hybrid) — selected persistent sidebar + slim top toolbar
- Defined desktop (260px/72px sidebar + 72px toolbar), tablet (auto-collapsed sidebar), and mobile (hamburger → sheet) layouts
- Specified navigation hierarchy with 4 groups: Overview, Collections, Account, Admin (conditional)
- Defined all 5 nav item states (default, hover, active, focus-visible, disabled) for expanded, collapsed, and mobile modes
- Designed breadcrumb alternative: sidebar active highlight + page-level back links + command palette recents
- Specified collapse/expand behavior: edge toggle, keyboard shortcut ([), localStorage persistence, 250ms transition with text fade timing
- Designed user menu: trigger from sidebar bottom + toolbar avatar, dropdown with profile/settings/theme/language/logout
- Specified top toolbar (search trigger, action buttons) and mobile header (hamburger, brand, quick actions)
- Documented responsive breakpoint summary and z-index layer map
- Files created: `tasks/design-layout.md`
- **Learnings for future iterations:**
  - Sidebar width tokens: 260px expanded, 72px collapsed — use these exact values in implementation
  - Mobile sheet width: 75vw max 288px — matches current implementation
  - Edge toggle button uses group-hover pattern: `group/sidebar` on aside, `group-hover/sidebar:opacity-100` on button
  - Nav item min-height 44px meets WCAG 2.5.8 target size requirement — don't reduce this
  - Breadcrumbs were deliberately excluded — use back links on detail pages instead (Linear/Notion pattern)
  - User menu placement differs by context: side="top" from sidebar, side="bottom" from toolbar
  - Theme toggle stays open after toggling (users may want multiple settings changes) — don't auto-close
---

## 2026-03-21 - US-006
- Designed enhanced command palette with full visual spec, result groups, animations, and responsive behavior
- Appended "Command Palette" section to `tasks/design-layout.md`
- Sections: Design Philosophy, Visual Design (backdrop/container/input/list/footer), Result Groups (Navigation/Collections/Recent/Actions/Settings), Result Item Layout, Item States, Keyboard Interactions, Search Behavior, Empty/No-Results States, Entrance/Exit Animation (Framer Motion), Responsive Behavior, Implementation Notes, Accessibility
- **Learnings for future iterations:**
  - cmdk's `CommandDialog` wraps Radix Dialog — animation needs `forceMount` + AnimatePresence pattern, not direct Framer Motion on CommandDialog
  - cmdk uses substring matching on `value` prop, not fuzzy — to get fuzzy, you'd need a custom filter function
  - Command palette positioned 25% from top (not centered) — matches Spotlight/Raycast convention and feels more intentional
  - Recent items should use `sessionStorage` (not localStorage) — recents should reset per session, not persist forever
  - Footer hint bar hidden on mobile — keyboard shortcut hints irrelevant on touch devices
  - Container uses `--radius-xl` (16px), larger than standard dialogs — the palette is a special surface that deserves extra polish
---

## 2026-03-21 - US-007
- Designed dashboard / home screen with full visual specification
- Created `tasks/design-pages.md` with "Dashboard" section
- Sections: Design Philosophy, Overall Page Layout, Greeting/Welcome Section, Stats Row (5 cards), Charts Section (pie + bar), Collection Summaries grid, Recent Acquisitions table, Empty State onboarding CTA, Loading State skeletons, Responsive Behavior Summary, Implementation Notes
- Time-aware greeting (morning/afternoon/evening) with dynamic subtitle
- Stats row: 5 stat cards with icon containers, animated numbers, hover lift, trend indicator placeholder
- Charts: donut pie (not full pie — modern feel) with custom legend + amber bar chart, both using CSS variable chart colors
- Collection summaries: 3-column card grid with gradient fallback covers, hover lift, keyboard accessibility
- Recent acquisitions: DataTable with 6 columns, condition badges, responsive column hiding
- Empty state: Sparkles icon with staggered reveal animation, CTA to create first collection
- Skeletons: Section-by-section layout matching final content structure
- **Learnings for future iterations:**
  - Charts must use `hsl(var(--chart-N))` for fills, not hardcoded hex — this is the dark mode fix for the audit finding
  - Donut chart (innerRadius + outerRadius) looks more modern than full pie — use for all future pie charts
  - Stats grid responsive: 5-col → 3-col → 2-col (not 5→2→1) to avoid orphan cards
  - Collection summary cards need `tabIndex={0}` + `role="link"` + `onKeyDown` for keyboard accessibility (audit gap)
  - Empty state should use `Sparkles` icon (inviting) not `Lock` icon (restrictive) — tone matters for onboarding
  - Max 6 collection cards on dashboard, max 8 recent acquisition rows — keeps page scannable
  - Section spacing is consistently `--space-8` (32px) desktop, `--space-6` (24px) mobile — use this as pattern for all pages
---

## 2026-03-21 - US-008
- Designed collections list page with full visual specification for grid view, list view, toolbar, dialogs, and responsive behavior
- Appended "Collections List" section to `tasks/design-pages.md`
- Sections: Design Philosophy, Grid View Card Design (container/image/overlay/badge/hover/press/actions/drag), List View Design (table/columns/states), Toolbar Design (search/filters/sort/view toggle/create), Create/Edit Dialog (form/dropzone/preview/error/buttons), Delete Confirmation, Empty State, Drag-to-Reorder, No Results State, Loading Skeleton, Responsive Summary, Implementation Notes
- Grid cards use 4:3 cinematic ratio with cover images as full-bleed backgrounds and gradient metadata overlay
- Added collection type badge (new element) for at-a-glance type identification in grid
- Hover state: -4px lift, shadow-lg, amber border glow, subtle image zoom
- Action buttons: frosted glass circles, hover-reveal with mobile always-visible override
- List view adds 40×40px thumbnail in name column (new enhancement)
- Drag-to-reorder visual: ring-2 ring-accent, shadow-xl, scale 1.02
- Create/edit dialog: cover image dropzone with live preview thumbnail (new)
- **Learnings for future iterations:**
  - Grid cards use `--radius-xl` (16px) not `--radius-lg` (12px) — collections page cards are intentionally larger radius for gallery feel
  - Gradient fallback rotation: `index % 4` cycles through 4 warm gradients from chart palette — prevents monotony when covers are missing
  - Collection type badge is always visible (not hover-dependent) — unlike action buttons which reveal on hover
  - Cover image zoom on hover should use CSS `transition: transform 400ms`, NOT Framer Motion — CSS handles continuous hover states more smoothly
  - List view thumbnail (40×40px) in name column is a new pattern — adds visual context not present in the current text-only DataTable
  - Drag handle and collection type badge share the top-left corner — when drag handle is present (custom sort), badge shifts to `left-12`
  - Card lift on hover is -4px (stronger than dashboard's -2px) — this is the hero page, cards should feel more responsive
  - Collection detail item cards use `--radius-lg` (12px) not `--radius-xl` — smaller items warrant tighter radius than collection gallery cards
  - Cover image banner with gradient overlay and metadata text: `bg-gradient-to-t from-black/70 via-black/30 to-transparent`
  - Gradient fallback for single collection cover: use `collection.id % 4` since there's no list index available
  - Mobile breakpoint moves banner action buttons below the banner — absolute positioning on small screens causes touch target issues
  - Set completion color thresholds: <50% muted, 50-99% accent, 100% success — gamification touch
---

## 2026-03-21 - US-009
- Designed collection detail page with full visual specification
- Appended "Collection Detail" section to `tasks/design-pages.md`
- Sections: Design Philosophy, Overall Page Layout, Back Navigation, Collection Header/Banner (cover image treatment, metadata overlay, action buttons), Tab Navigation, Items Tab (toolbar, grid view, list view, empty state, drag-to-reorder), Sets Tab (accordion expansion, progress bars, CRUD), Stats Tab (stat cards, charts), Add Item Dialog, Export Dialog, Import Dialog, Loading State, Error/Not Found State, Responsive Summary, Implementation Notes
- Key decisions: cover image as full-width cinematic banner with frosted glass overlay, tab-based content organization, stats embedded in banner, item cards use --radius-lg (12px) vs collection cards' --radius-xl (16px), set completion uses color thresholds for gamification
- **Learnings for future iterations:**
  - Collection detail item cards use `--radius-lg` (12px), not `--radius-xl` (16px) — items are denser/smaller than collection gallery cards
  - Cover image banner uses `bg-gradient-to-t from-black/70 via-black/30 to-transparent` for text readability on any image
  - Gradient fallback for a single collection: `collection.id % 4` (no list index available, unlike collections list page)
  - Frosted glass buttons on banners: `bg-white/20 backdrop-blur-sm border-white/30 text-white hover:bg-white/30`
  - Mobile adaptation moves absolute-positioned banner buttons to below the banner — absolute positioning on small touch screens causes target issues
  - Set accordion expand uses Framer Motion `animate={{ height: "auto" }}` — requires measuring content; consider `layout` prop
  - Completion color thresholds (<50% muted, 50-99% accent, 100% success) — deliberate gamification for set tracking
  - Stats tab reuses `StatCard` from dashboard — keep consistent card design across pages
  - Tab content animation should be lightweight (fade + small y) to avoid feeling slow on frequent tab switches
---

## 2026-03-21 - US-010
- Designed catalog item detail page with full visual specification
- Appended "Catalog Item Detail" section to `tasks/design-pages.md`
- Sections: Design Philosophy, Overall Page Layout, Back Navigation (breadcrumb trail), Item Hero Layout (image panel + identity panel), Metadata Display, Custom Fields Card, Set Membership Indicator, Owned Copies Section (empty state, copy card design), Add/Edit Owned Copy Flow, Loading State (skeleton), Error/Not Found State, Responsive Summary, Implementation Notes
- Key decisions: side-by-side hero (2/5 image + 3/5 info) like a product detail page, breadcrumb navigation (only page with full breadcrumbs — 3 levels deep justifies it), condition badge color semantics (Mint/NearMint→success, Excellent/Good→primary, Fair/Poor→warning), set membership as contextual callout cards with completion color thresholds
- **Learnings for future iterations:**
  - This page uses breadcrumbs unlike other pages (which use sidebar + back links) — warranted because user is 3 levels deep
  - Copy card action buttons need `sm:opacity-0 sm:group-hover:opacity-100` (not just `opacity-0`) for mobile touch support
  - Image fallback on item detail is simpler than collection cards — just `bg-muted` with centered icon, no gradient rotation needed (single item, not a gallery)
  - Edit item dialog uses `sm:max-w-2xl` (wider than copy dialog's `sm:max-w-lg`) — more fields including custom field grid
  - Set membership section is designed but depends on API returning set data for individual items — design is ready, implementation gated on API
  - Financial values in copy cards should use `font-variant-numeric: tabular-nums` for decimal alignment
- Lightbox component is NEW — `ImageGalleryLightbox` in `components/ds/`, portal-rendered, keyboard-trapped
- ImageUploadZone is NEW — encapsulates drag-drop zone, file validation, preview grid, reorder via @dnd-kit
- Inline editing uses optimistic updates with rollback — no toast on success, toast on error
- Only Condition and Purchase Price support inline edit — other fields use full edit dialog

---

## 2026-03-21 - US-011
- Designed owned copies and image gallery with full visual specification
- Appended "Owned Copies" section to `tasks/design-pages.md`
- Sections: Design Philosophy, Copy Card Design (detailed layout/container/badge/actions/details/notes/thumbnails), Image Gallery Lightbox (visual design/navigation/keyboard/touch/zoom/responsive), Create/Edit Owned Copy Form (enhanced dialog with color-coded condition select, currency-prefixed inputs, textarea notes, drag-and-drop image upload zone with preview grid and reorder), Delete Confirmation (copy-specific), Inline Editing Pattern (condition dropdown + price inline input with optimistic updates), Responsive Summary, Implementation Notes
- Key decisions: condition badge as hero visual element with 3-tier color semantics, thumbnail overflow indicator (+N badge), lightbox with direction-aware slide transitions and swipe/zoom support, multi-image upload zone replacing bare file input, inline editing limited to condition and price (most commonly updated), textarea for notes (upgrade from text input)
- Two new components designed: `ImageGalleryLightbox` and `ImageUploadZone` for `components/ds/`
- **Learnings for future iterations:**
  - Lightbox needs `createPortal` to escape overflow containers — don't render inside card content
  - Focus must return to trigger thumbnail after lightbox close — store ref before opening
  - Inline editing: optimistic update + rollback pattern avoids perceived latency; no success toast needed for inline saves
  - Image upload preview grid uses `grid-cols-4 sm:grid-cols-5` — NOT flex-wrap (prevents ragged last row)
  - Existing image removal in edit mode needs mini-confirmation (tooltip popover) — accidental removal is destructive and not undoable
  - Copy cards should NOT have lift hover effect (unlike collection gallery cards) — copies are data records, not navigable destinations
  - Thumbnail overflow max is 4 desktop / 3 mobile — prevents card height bloat with many images
  - `group-focus-within:opacity-100` ensures hover-reveal action buttons appear when keyboard-focused (not just mouse hover)
  - Currency prefix on price inputs: `absolute left-3` with `pl-7` on input — consistent with SaaS form patterns
---

## 2026-03-21 - US-012
- Designed sets page and set detail with full visual specification
- Appended "Sets" section to `tasks/design-pages.md`
- Sections: Design Philosophy, Sets List View (enhanced card design), Completion Visualization (circular progress ring as hero element), Set Card Expanded State (item checklist with owned/missing distinction), Set Detail Page (hero header with large ring + progress bar, filter tabs, item card grid), Quick Action "Add to Wishlist", Empty States (no sets, no items, all owned celebration), Loading Skeletons, Responsive Summary, Implementation Notes
- Key decisions: circular progress ring (not bar) as signature visual — 36px in cards, 64px in detail hero; three completion tiers with distinct colors (<50% muted, 50-99% amber, 100% success green with glow); missing items use dashed border cards; owned items get solid border with green left stripe; "Add to Wishlist" as first-class Heart button on missing items with optimistic UI; set detail as sub-route `/collections/:collectionId/sets/:setId`
- New component designed: `SetProgressRing` for `components/ds/` — SVG-based, CSS-animated, two sizes
- **Learnings for future iterations:**
  - Progress ring uses raw SVG with `stroke-dasharray` — no chart library needed, CSS transition handles animation
  - `stroke-dasharray` calculation: circumference = 2πr, fill = (percentage/100) * circumference
  - Ring starts from 12 o'clock via `transform: rotate(-90deg)` on the SVG element
  - Dashed border (`border-dashed`) is a deliberate visual language: dashed = incomplete slot, solid = acquired
  - Missing item cards use `bg-card/50` (semi-transparent) to visually recede behind owned items
  - Owned items get `border-l-3 border-l-success` left stripe — subtle but clear ownership signal
  - 100% completion celebration: green glow via `filter: drop-shadow()` + one-time scale pulse via Framer Motion spring
  - Wishlist quick-add uses optimistic update: button → "Added" (success) for 2s → "In Wishlist" (disabled)
  - Set detail filter tabs are local state, not URL params — sets are small enough for client-side filtering
  - Set items determine owned/missing by cross-referencing `catalogItemId` with collection's owned items
---

## 2026-03-21 - US-013
- Designed wishlist page with full visual specification
- Appended "Wishlist" section to `tasks/design-pages.md`
- Sections: Design Philosophy, Overall Page Layout, Grouped Layout (collapsible collection headers with chevron rotation animation), Priority Badge Design (3-tier color system with semantic badges), Wishlist Item Card (compact data card with drag handle, name, priority badge, target price, notes preview, catalog link), Actions Dropdown (hover-reveal 3-dot menu), Filter and Sort Toolbar (priority filter, sort by, date added filter, item count), Create/Edit Dialog (collection select, name input, priority+price grid, notes textarea upgrade, catalog item search), Delete Confirmation, Drag-to-Reorder, Empty State (Heart icon, encouraging copy), No Results State (Search icon, filter hint), Loading Skeleton, Responsive Summary, Implementation Notes
- Key decisions: cards use subtle `hover:shadow-md` without translate lift (data cards, not destinations — matches copy card pattern); notes upgraded from Input to Textarea for multi-line editing; chevron rotation via single icon + Framer Motion rotate (smoother than icon swap); priority badges use existing semantic color system (destructive/accent/default); new date-added filter for frequent wishlisting users; currency prefix pattern on price input matches owned copy forms; catalog link as "View in catalog →" text link within card
- New enhancements beyond current implementation: date added filter (client-side), notes textarea (was Input), priority color dots in filter selects, no-results state with Search icon (was plain text), catalog link in card body (was icon only in header)
- **Learnings for future iterations:**
  - Wishlist cards should NOT lift on hover (no translateY) — they are data cards like copy cards, not navigable destinations like collection gallery cards
  - Chevron animation: rotate a single ChevronDown icon (-90° collapsed, 0° expanded) instead of swapping ChevronDown/ChevronRight — one icon is smoother
  - Notes fields benefit from Textarea over Input — collectors write multi-line notes about condition requirements, pricing history, etc.
  - Priority color dots in Select options require inline flex layout inside SelectItem — can't rely on text-only content
  - Date-range client-side filtering can use `item.id` ordering as creation-date proxy when API doesn't return `createdAt`
  - Group collapse state should NOT be persisted to localStorage — wishlist groups are small and should default to all-expanded on each visit
  - Filter "no results" state needs visual treatment (icon + hint text) distinct from "empty" state (EmptyState component + action CTA)
---
