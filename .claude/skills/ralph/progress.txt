## Codebase Patterns
- i18n: All UI strings go in both `src/web/src/i18n/locales/en.json` and `pt.json` — nested keys under feature namespaces
- Typecheck: `cd src/web && npx tsc -b` must pass before committing
- Frontend tests: `cd src/web && npm test` to run all tests

---

## 2026-03-15 - US-001
- Added i18n keys for sidebar and toolbar redesign to both en.json and pt.json
- Keys added: `app.tagline`, `nav.groups.*`, `nav.userMenu.*`, `toolbar.*`
- Files changed: `src/web/src/i18n/locales/en.json`, `src/web/src/i18n/locales/pt.json`
- **Learnings for future iterations:**
  - The locale files are plain JSON with nested namespaces — follow existing grouping conventions
  - Both locale files must stay in sync — same keys, translated values
---

## 2026-03-15 - US-002
- DropdownMenu component already existed at `src/web/src/components/ds/dropdown-menu.tsx`
- It wraps `@radix-ui/react-dropdown-menu` (already installed) and exports all required primitives
- Barrel export in `ds/index.ts` already includes all DropdownMenu exports
- No code changes needed — verified existing implementation meets all acceptance criteria
- Files changed: none (just PRD update)
- **Learnings for future iterations:**
  - The `components/ds/` directory is where shadcn/ui-style components live (not `components/ui/`)
  - `components/ui/` has lower-level primitives (button, dialog, input, label, sheet)
  - Always check existing components before creating new ones — the PRD notes hint at this
---

## 2026-03-15 - US-003
- Replaced flat `navItems` array with exported `navGroups: NavGroup[]` structure in sidebar.tsx
- Three groups: Overview (Dashboard), Collections (Collections, Collection Types, Wishlist), Account (Profile)
- Both `Sidebar` and `MobileSidebarContent` now iterate over `navGroups` instead of `navItems`
- Added `NavItem` and `NavGroup` interfaces
- No functional changes — same nav items render in the same order
- Files changed: `src/web/src/components/layout/sidebar.tsx`
- **Learnings for future iterations:**
  - `navGroups` is exported from sidebar.tsx for shared use
  - Each group has a `labelKey` (i18n) and `items` array
  - Both desktop and mobile sidebar components consume the same data structure
---

## 2026-03-15 - US-004
- Rebuilt sidebar header to show vault icon + "GeekVault" title (font-display) + tagline subtitle (i18n `app.tagline`)
- Header height changed from h-14 to h-[72px] with proper vertical centering
- Collapsed mode shows only vault icon centered; expanded shows icon + text block with gap-3
- Removed logoFull image usage from desktop sidebar (still used by MobileSidebarContent)
- Files changed: `src/web/src/components/layout/sidebar.tsx`
- **Learnings for future iterations:**
  - `font-display` maps to Nunito font (defined in index.css as `--font-display`)
  - `vaultIcon` asset is the small square icon, `logoFull` is the full horizontal logo image
  - Sidebar header and top toolbar should both be h-[72px] for visual alignment
---

## 2026-03-15 - US-005
- Updated sidebar dimensions: expanded w-[260px], collapsed w-[72px]
- Nav items now min-h-[44px] for comfortable touch targets
- Icons increased from h-4 w-4 to h-5 w-5 for all nav items
- Gap between navigation groups changed to gap-4 (16px)
- Applied to both Sidebar and MobileSidebarContent
- Files changed: `src/web/src/components/layout/sidebar.tsx`
- **Learnings for future iterations:**
  - Sidebar widths are only defined in sidebar.tsx — no external references to update
  - The nav group wrapping div with gap-4 also prepares for group labels in US-006
---

## 2026-03-15 - US-006
- Added group labels (text-xs, uppercase, muted) above each navigation group in expanded mode
- Collapsed mode shows thin horizontal separator lines between groups instead of labels
- Mobile sidebar also shows group labels (always expanded)
- Files changed: `src/web/src/components/layout/sidebar.tsx`
- **Learnings for future iterations:**
  - Collapsed mode uses border-t separators between groups instead of text labels
  - Group labels use `font-semibold uppercase text-sidebar-foreground/50`
---

## 2026-03-15 - US-007
- Active nav item now has 3px left border (border-l-[3px]) instead of 2px
- Active icon colored with sidebar-primary color
- Hover state uses lighter bg (bg-sidebar-accent/50) without left border — distinct from active
- Used NavLink render function children pattern to conditionally color icons
- Applied to both Sidebar and MobileSidebarContent
- Files changed: `src/web/src/components/layout/sidebar.tsx`
- **Learnings for future iterations:**
  - NavLink in react-router-dom v7 supports both `className` and `children` as render functions simultaneously
  - Use `{({ isActive }) => (...)}` as children to conditionally style inner elements
---

## 2026-03-15 - US-008
- All acceptance criteria already met by previous stories (US-005, US-006, US-007)
- Collapsed width 72px, centered icons h-5 w-5, tooltips on right, active state with bg + 3px border + colored icon, group separators
- No code changes needed
- Files changed: none (just PRD update)
---
