## Codebase Patterns
- Backend: .NET 8.0 minimal API with EF Core + SQL Server. Build: `cd src/api/GeekVault.Api && dotnet build`. Test: `cd src/api/GeekVault.Api.Tests && dotnet test`
- Frontend: React 19 + TypeScript + Vite 8 + Tailwind CSS v4. Build: `cd src/web && npx tsc -b`. Test: `cd src/web && npm test`
- i18n: All UI strings go in both `src/web/src/i18n/locales/en.json` and `pt.json`
- Existing patterns to follow: CatalogItemsService.ReorderAsync for reorder logic, SetsService for completion % calculation
- CollectionResponse is a positional record — all constructor call sites are in CollectionsService only (now includes CollectionTypeName)
- Use GetByIdAndUserIdWithTypeAsync (not GetByIdAndUserIdAsync) when you need CollectionType.Name in the response
- CollectionWithCounts DTO in ICollectionsRepository is the projected query pattern for GetAll — extend it for new fields (includes ItemCount + OwnedCount)
- OwnedCopies count subquery pattern: `_db.OwnedCopies.Count(oc => _db.CatalogItems.Any(ci => ci.Id == oc.CatalogItemId && ci.CollectionId == c.Id))`
- Owned copies endpoint: POST /api/items/{catalogItemId}/copies (requires Condition field)
- CollectionResponse now includes OwnedCount + CompletionPercentage — all constructor call sites updated in CollectionsService
- EF Core migrations: use `defaultValueSql: "GETUTCDATE()"` instead of `defaultValue` for UTC now defaults on existing rows
- Catalog items endpoint: POST /api/collections/{collectionId}/items (requires Identifier field)
- Both repositories share the same scoped DbContext — setting UpdatedAt on a Collection loaded by one repo and calling SaveChangesAsync on another repo persists all tracked changes
- Collections reorder endpoint: POST /api/collections/reorder with `{ CollectionIds: [int] }` — default sort is now sortOrder (not name), use `?sortBy=name` for explicit alphabetical

---

## 2026-03-15 - US-001
- Added CreatedAt (DateTime, default UTC now) and UpdatedAt (DateTime?, nullable) to Collection entity
- Updated CollectionResponse DTO to include both timestamp fields
- Set CreatedAt on CreateAsync, UpdatedAt on UpdateAsync and UploadCoverAsync
- Generated EF Core migration with GETUTCDATE() default for existing rows
- All 128 integration tests pass, build succeeds
- Files changed:
  - src/api/GeekVault.Api/Entities/Vault/Collection.cs
  - src/api/GeekVault.Api/DTOs/Vault/CollectionDtos.cs
  - src/api/GeekVault.Api/Services/Vault/CollectionsService.cs
  - src/api/GeekVault.Api/Migrations/20260315234848_AddCollectionTimestamps.cs
  - src/api/GeekVault.Api/Migrations/20260315234848_AddCollectionTimestamps.Designer.cs
- **Learnings for future iterations:**
  - EF Core migration default values use `defaultValue` which sets a C# literal — use `defaultValueSql: "GETUTCDATE()"` for UTC now defaults on existing rows
  - CollectionResponse is a positional record in CollectionDtos.cs — adding fields requires updating all constructor call sites in CollectionsService
  - All CollectionResponse constructors are in CollectionsService only (no other services create them)
---

## 2026-03-15 - US-002
- Propagated UpdatedAt to parent Collection from CatalogItemsService (CreateAsync, DeleteAsync, ReorderAsync) and OwnedCopiesService (CreateAsync, DeleteAsync)
- For CatalogItemsService: collection already loaded via _collectionsRepository — just set collection.UpdatedAt before SaveChangesAsync
- For OwnedCopiesService: item.Collection already loaded via GetCatalogItemWithCollectionAsync (Include) — set item.Collection.UpdatedAt before SaveChangesAsync
- All 128 integration tests pass, build succeeds
- Files changed:
  - src/api/GeekVault.Api/Services/Vault/CatalogItemsService.cs
  - src/api/GeekVault.Api/Services/Vault/OwnedCopiesService.cs
- **Learnings for future iterations:**
  - Both repositories share the same scoped DbContext — setting a property on an entity loaded by one repo and calling SaveChangesAsync on another repo persists all tracked changes
  - OwnedCopiesRepository.GetCatalogItemWithCollectionAsync includes Collection navigation property, so item.Collection is available for direct mutation
---

## 2026-03-15 - US-003
- Added CollectionTypeName (string) to CollectionResponse DTO
- Added .Include(c => c.CollectionType) to GetByUserIdAsync in CollectionsRepository
- Created GetByUserIdWithCountsAsync projected query in repository — returns CollectionWithCounts (Collection + ItemCount) in single DB round-trip, eliminating N+1
- Updated GetAllAsync to use projected query instead of per-collection GetItemCountAsync loop
- Updated GetByIdAsync and UpdateAsync to use GetByIdAndUserIdWithTypeAsync for CollectionType.Name
- Updated CreateAsync to reload with type after save
- All 128 integration tests pass, build succeeds
- Files changed:
  - src/api/GeekVault.Api/DTOs/Vault/CollectionDtos.cs
  - src/api/GeekVault.Api/Repositories/Vault/ICollectionsRepository.cs
  - src/api/GeekVault.Api/Repositories/Vault/CollectionsRepository.cs
  - src/api/GeekVault.Api/Services/Vault/CollectionsService.cs
- **Learnings for future iterations:**
  - CollectionWithCounts in ICollectionsRepository is the projected query DTO — extend it for additional computed fields (OwnedCount for US-005)
  - GetByIdAndUserIdWithTypeAsync already existed and includes CollectionType — use it when CollectionType.Name is needed
  - After AddAsync+SaveChangesAsync, navigation properties aren't loaded — reload with GetByIdAndUserIdWithTypeAsync to get CollectionType.Name
---

## 2026-03-16 - US-004
- Added server-side sort to GET /api/collections with sortBy (name | updatedAt | itemCount | createdAt) and sortDir (asc | desc) query params
- Default sort: name ascending
- Sorting performed in DB query via IQueryable OrderBy/OrderByDescending in the repository projected query
- For updatedAt sort, uses coalesce (UpdatedAt ?? CreatedAt) so collections without updates sort by creation date
- Added 6 integration tests covering each sort option and default behavior
- All 134 tests pass, build succeeds
- Files changed:
  - src/api/GeekVault.Api/Controllers/Vault/CollectionsController.cs
  - src/api/GeekVault.Api/Repositories/Vault/ICollectionsRepository.cs
  - src/api/GeekVault.Api/Repositories/Vault/CollectionsRepository.cs
  - src/api/GeekVault.Api/Services/Vault/ICollectionsService.cs
  - src/api/GeekVault.Api/Services/Vault/CollectionsService.cs
  - src/api/GeekVault.Api.Tests/CollectionEndpointsTests.cs
- **Learnings for future iterations:**
  - Catalog items endpoint is POST /api/collections/{collectionId}/items (not /api/catalog-items) and requires Identifier field
  - Sort in repository: use switch expression on sortBy with ToLowerInvariant() for case-insensitive matching
  - When filtering dotnet test by name, coverage gate may fail due to low coverage — run all tests for final validation
---

## 2026-03-16 - US-005
- Added OwnedCount (int) and CompletionPercentage (double, 0-100) to CollectionResponse DTO
- Extended CollectionWithCounts with OwnedCount field, added OwnedCopies.Count() subquery to projected query in GetByUserIdWithCountsAsync
- Added GetOwnedCountAsync to ICollectionsRepository for single-collection lookups (GetByIdAsync, UpdateAsync)
- CompletionPercentage calculated as Math.Round((double)ownedCount / itemCount * 100, 1), 0 when itemCount is 0
- Added 2 integration tests: GetCollection_ReturnsCompletionPercentage, ListCollections_ReturnsCompletionPercentage
- All 136 tests pass, build succeeds
- Files changed:
  - src/api/GeekVault.Api/DTOs/Vault/CollectionDtos.cs
  - src/api/GeekVault.Api/Repositories/Vault/ICollectionsRepository.cs
  - src/api/GeekVault.Api/Repositories/Vault/CollectionsRepository.cs
  - src/api/GeekVault.Api/Services/Vault/CollectionsService.cs
  - src/api/GeekVault.Api.Tests/CollectionEndpointsTests.cs
- **Learnings for future iterations:**
  - OwnedCopies count subquery: `_db.OwnedCopies.Count(oc => _db.CatalogItems.Any(ci => ci.Id == oc.CatalogItemId && ci.CollectionId == c.Id))` — same pattern used in GetCollectionSummariesAsync
  - Owned copies endpoint: POST /api/items/{catalogItemId}/copies (requires Condition field)
  - Test record types (e.g. CollectionResult) must be updated when adding new fields to response DTOs — deserialization silently ignores extra fields but missing positional params cause issues
---

## 2026-03-16 - US-006
- Added POST /api/collections/{id}/cover-from-item/{itemId} endpoint that copies a catalog item's image as the collection cover
- Injected ICatalogItemsRepository into CollectionsService to look up items by id and collection
- Implementation: validates collection ownership + item belongs to collection + item has image, then File.Copy to new cover path
- Sets UpdatedAt on the collection after copying
- Returns 404 if collection not found, item not in collection, or item has no image
- Added 3 integration tests: happy path, item-not-in-collection, item-has-no-image
- All 139 tests pass, build succeeds, 94.54% line coverage
- Files changed:
  - src/api/GeekVault.Api/Services/Vault/ICollectionsService.cs
  - src/api/GeekVault.Api/Services/Vault/CollectionsService.cs
  - src/api/GeekVault.Api/Controllers/Vault/CollectionsController.cs
  - src/api/GeekVault.Api.Tests/CollectionEndpointsTests.cs
- **Learnings for future iterations:**
  - Catalog item image upload endpoint: POST /api/collections/{collectionId}/items/{id}/image (multipart form, field name "image")
  - CatalogItem.Image stores the relative URL (e.g. "/uploads/item-123.png") — use Path.Combine(webRootPath, image.TrimStart('/')) to get the filesystem path
  - CollectionsService now has ICatalogItemsRepository injected — can look up items directly without going through CatalogItemsService
---

## 2026-03-16 - US-007
- Added SortOrder (int, default 0) to Collection entity
- Generated EF Core migration AddCollectionSortOrder (default 0 for existing rows)
- Added ReorderCollectionsRequest DTO in CollectionDtos.cs
- Added GetByIdsAndUserIdAsync to ICollectionsRepository + CollectionsRepository
- Added ReorderAsync to ICollectionsService + CollectionsService (follows CatalogItemsService.ReorderAsync pattern)
- Added POST /api/collections/reorder endpoint to CollectionsController
- Changed default sort (no sortBy param) from name to sortOrder with ThenBy name as tiebreaker
- Added explicit "name" case to sort switch so `?sortBy=name` still sorts alphabetically
- Added 2 integration tests: ReorderCollections_UpdatesSortOrder, ReorderCollections_DuplicateIds_ReturnsBadRequest
- All 141 tests pass, 94.59% line coverage, build succeeds
- Files changed:
  - src/api/GeekVault.Api/Entities/Vault/Collection.cs
  - src/api/GeekVault.Api/DTOs/Vault/CollectionDtos.cs
  - src/api/GeekVault.Api/Repositories/Vault/ICollectionsRepository.cs
  - src/api/GeekVault.Api/Repositories/Vault/CollectionsRepository.cs
  - src/api/GeekVault.Api/Services/Vault/ICollectionsService.cs
  - src/api/GeekVault.Api/Services/Vault/CollectionsService.cs
  - src/api/GeekVault.Api/Controllers/Vault/CollectionsController.cs
  - src/api/GeekVault.Api/Migrations/20260316001217_AddCollectionSortOrder.cs
  - src/api/GeekVault.Api.Tests/CollectionEndpointsTests.cs
- **Learnings for future iterations:**
  - Default sort is now sortOrder (not name) — explicit `?sortBy=name` still works
  - ReorderCollectionsRequest uses CollectionIds (not ItemIds) — follows same pattern as ReorderItemsRequest but for collections
  - When all SortOrder values are 0 (initial state), ThenBy Name ensures alphabetical tiebreaking
---

## 2026-03-16 - US-008
- Removed description prop from PageHeader on collections page (both loading and main views)
- New Collection button remains in the actions slot, right-aligned
- Added i18n keys to both en.json and pt.json: collections.sort.{name,lastUpdated,mostItems,recentlyAdded}, collections.toolbar.{filters,sortBy}, collections.viewGrid, collections.viewList, collections.updated, collections.complete, collections.useAsCover
- All 514 frontend tests pass, typecheck passes
- Files changed:
  - src/web/src/features/collections/collections-page.tsx
  - src/web/src/i18n/locales/en.json
  - src/web/src/i18n/locales/pt.json
- **Learnings for future iterations:**
  - i18n keys for collections toolbar/grid are nested under collections.sort.*, collections.toolbar.*, and flat under collections.viewGrid/viewList/updated/complete/useAsCover
  - The collections.description key still exists in locale files (no breaking change) but is no longer rendered on the page
---

## 2026-03-16 - US-009
- Built unified toolbar with search (max-width 420px), type filter dropdown, sort dropdown, and New Collection button (right-aligned via ml-auto)
- Search preserves existing debounced behavior
- Type filter: All Types (default) + all collection types from API
- Sort dropdown: Name (asc), Last Updated (desc), Most Items (desc), Recently Added (desc) — triggers server-side sort via API query params
- Sort preference (sortBy + sortDir) persisted to localStorage (keys: collections-sortBy, collections-sortDir) and restored on page load
- Default sort: name ascending
- Moved New Collection button from header actions slot into toolbar
- fetchCollections now passes sortBy/sortDir query params to GET /api/collections
- All 514 frontend tests pass, typecheck passes
- Files changed:
  - src/web/src/features/collections/collections-page.tsx
- **Learnings for future iterations:**
  - Sort dropdown value uses composite format "sortBy:sortDir" (e.g. "name:asc") to encode both sort field and direction in a single Select value
  - localStorage keys: collections-sortBy, collections-sortDir — used by sort dropdown persistence
  - fetchCollections accepts optional sort override param for immediate re-fetch when sort changes (avoids stale closure)
---

## 2026-03-16 - US-010
- Restructured toolbar for mobile collapsibility
- Mobile (< sm): search full-width + Filters toggle button; tapping reveals/hides type filter + sort row with CSS grid animation
- Desktop (>= sm): all toolbar items shown inline, no toggle
- New Collection button always visible in both layouts
- Animation uses CSS grid-rows-[0fr]/[1fr] transition for smooth collapse/expand
- Added SlidersHorizontal icon from lucide-react for Filters toggle
- All 514 frontend tests pass, typecheck passes
- Files changed:
  - src/web/src/features/collections/collections-page.tsx
- **Learnings for future iterations:**
  - CSS grid-rows-[0fr]/[1fr] with overflow-hidden is a clean way to animate height collapse without JS measurement
  - Mobile filter selects are duplicated (desktop hidden:sm, mobile sm:hidden) to avoid complex conditional rendering — the Select mock handles both
  - filtersOpen state controls mobile filter row visibility
---

## 2026-03-16 - US-011
- Replaced old Card/CardContent/CardFooter/Badge markup with Netflix-style cover card
- Cover card: aspect-ratio 4/3, rounded-xl, overflow hidden, full-bleed cover image with object-cover
- Gradient overlay at bottom: linear-gradient(transparent, rgba(0,0,0,0.7)) with collection name (18px semibold white) and metadata (13px white/85%)
- Fallback: gradient placeholder with Library icon when no cover image
- Entire card clickable, navigates to /collections/{id}
- Three-dot menu in top-right corner, visible on hover with backdrop blur
- Removed: Card, CardContent, CardFooter, Badge, Eye imports (no longer needed)
- Updated loading skeleton to match new 4/3 aspect ratio card shape
- Updated test: removed "Comic Books" badge assertion (type badge removed from card)
- All 514 frontend tests pass, typecheck passes
- Files changed:
  - src/web/src/features/collections/collections-page.tsx
  - src/web/src/features/collections/collections-page.test.tsx
- **Learnings for future iterations:**
  - Cover card uses inline style for aspect-ratio 4/3 and gradient background
  - Three-dot menu uses bg-black/30 backdrop-blur-sm for readability over images
  - collectionTypeName is no longer displayed on the card (removed Badge) — only shown in list view (US-015)
---

## 2026-03-16 - US-012
- Added ownedCount, completionPercentage, createdAt, updatedAt to Collection interface
- Built getRelativeTime() using Intl.RelativeTimeFormat (no date-fns dependency)
- Built getMetadataLine() that joins: '{count} items · {percent}% complete · Updated {timeAgo}'
- If itemCount is 0, completion % is omitted; if updatedAt is null, time is omitted
- Metadata styled: 13px, white text, 85% opacity (text-[13px] text-white/85)
- Updated test fixtures with new fields, updated metadata assertion to use content matcher
- All 514 frontend tests pass, typecheck passes
- Files changed:
  - src/web/src/features/collections/collections-page.tsx
  - src/web/src/features/collections/collections-page.test.tsx
- **Learnings for future iterations:**
  - Intl.RelativeTimeFormat used without locale param (defaults to browser locale) — matches i18n language automatically
  - Metadata line uses " · " (middle dot) as separator between parts
  - Test uses content matcher function for metadata: `screen.getByText((content) => content.includes(...))`
---

## 2026-03-16 - US-013
- Added hover lift: translateY(-4px) and shadow-lg via Tailwind hover:-translate-y-1 hover:shadow-lg with 200ms ease transition
- Quick action buttons appear on hover: Open (ExternalLink), Edit (Pencil), three-dot menu (MoreVertical)
- All quick actions have semi-transparent backdrop (bg-black/30 backdrop-blur-sm)
- On touch devices (max-[640px]): quick actions always visible via max-[640px]:opacity-100
- Three-dot menu provides Delete option
- Updated tests: edit buttons now found via getAllByLabelText instead of getAllByText
- All 514 frontend tests pass, typecheck passes
- Files changed:
  - src/web/src/features/collections/collections-page.tsx
  - src/web/src/features/collections/collections-page.test.tsx
- **Learnings for future iterations:**
  - Quick action buttons use aria-label for accessibility — tests should use getAllByLabelText
  - Touch device visibility uses max-[640px]:opacity-100 (always show on narrow screens)
  - Edit is now a direct icon button, not inside dropdown — dropdown only has Delete
---
